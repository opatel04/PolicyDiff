// Owner: Dominic
// PolicyList — searchable, filterable table of all ingested policies.
// TODO: call listPolicies() with filter params (payer, drug, status)
// TODO: implement table with sortable columns
// TODO: link each row to policy detail

export default function ExplorerPage() {
    return (
        <div className="p-8">
            <h2 className="text-3xl font-bold tracking-tight mb-2">Policy Explorer</h2>
            <p className="text-muted-foreground">Searchable, filterable table of all ingested policies.</p>
            {/* TODO: implement policy list table */}
        </div>
    );
}
