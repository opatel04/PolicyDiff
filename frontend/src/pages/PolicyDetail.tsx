// Owner: Dominic
// PolicyDetail — full policy record with extracted drug criteria.
// TODO: call getPolicy(id) and getPolicyCriteria(id) using :id from route params
// TODO: display structured criteria (step therapy, PA requirements, quantity limits)
// TODO: add "Compare" CTA linking to ComparisonMatrix with this policy pre-selected

export default function PolicyDetail() {
  return (
    <div className="p-8 text-white">
      <p>PolicyDetail — Owner: Dominic</p>
      {/* TODO: implement policy detail view */}
    </div>
  );
}
